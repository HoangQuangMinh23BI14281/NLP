\section{Experiments \& Results}

\subsection{Experimental Setup}

\paragraph{Dataset and Preprocessing.}
We use a custom NER dataset stored in JSONL format with token-level and tag-level annotations.
The dataset contains 11,001 training examples, which we split into training and test sets with an 80/20 ratio
(8,800 training, 2,201 test) using a fixed random seed of 42 for reproducibility.

Each example consists of a sequence of tokens and corresponding NER tags. During preprocessing, we encode
all unique tags into numeric IDs and compute class weights based on tag frequency in the training set to
address label imbalance. These weights are used in Focal Loss variants.

Tokenization is performed using model-specific \texttt{AutoTokenizer} with the \texttt{is\_split\_into\_words=True}
flag to handle word-to-subword alignment. For multi-subword tokens, we assign the label to the first subword
and mask subsequent subwords with a special ignore index (\texttt{-100}) to exclude them from loss and metric computation.

\paragraph{Training Hyperparameters.}
We train all models with consistent hyperparameters across architectures:
\begin{itemize}
  \item \textbf{Learning rate:} $2 \times 10^{-5}$ (standard for fine-tuning pre-trained Transformers)
  \item \textbf{Batch size:} 16 (training and evaluation)
  \item \textbf{Number of epochs:} 5
  \item \textbf{Weight decay:} 0.01 (L2 regularization)
  \item \textbf{Evaluation strategy:} per-epoch
  \item \textbf{Optimizer:} AdamW (default in Hugging Face Trainer)
\end{itemize}

\paragraph{Loss Functions and Training Objectives.}
We compare six training objectives:

\begin{enumerate}
  \item \textbf{Base\_CE (Baseline):} Standard cross-entropy loss applied to token classification logits.
  
  \item \textbf{Focal Loss:} Weighted focal loss to handle class imbalance:
  \begin{equation}
    \text{FL}(p_t) = -\alpha_t (1 - p_t)^\gamma \log(p_t)
  \end{equation}
  where $p_t$ is the model probability for the correct class, $\gamma = 2.0$ is the focusing parameter, and $\alpha_t$ is the class weight computed from training tag frequencies.
  
  \item \textbf{Dice Loss:} A soft intersection-over-union loss commonly used in segmentation:
  \begin{equation}
    \text{DiceLoss} = 1 - \frac{2 \sum_c p_c y_c + \epsilon}{\sum_c p_c + y_c + \epsilon}
  \end{equation}
  where $p_c$ and $y_c$ are softmax probabilities and one-hot targets per class, and $\epsilon = 10^{-6}$ is a smoothing constant.
  
  \item \textbf{CE + Dice Loss:} A combined loss summing cross-entropy and Dice loss to leverage both.
  
  \item \textbf{CRF:} A Conditional Random Field layer replacing softmax decoding to model label transitions explicitly. The CRF learns transition scores between tag pairs and finds the maximum-likelihood sequence during inference.
  
  \item \textbf{CRF + Focal Loss:} CRF decoding with sequence-level Focal Loss applied to the CRF negative log-likelihood.
\end{enumerate}

\paragraph{Evaluation Metrics.}
We evaluate all models using the \texttt{seqeval} metric, which computes entity-level metrics:
\begin{itemize}
  \item \textbf{Precision:} proportion of predicted entities that are correct.
  \item \textbf{Recall:} proportion of gold entities that are predicted correctly.
  \item \textbf{F1-Score:} harmonic mean of precision and recall.
  \item \textbf{Accuracy:} token-level accuracy (including padding/masked tokens).
\end{itemize}
Metrics are computed on the test set after each epoch, and we report the final epoch results.


\subsection{BERT results}

\subsection{RoBERTa results}

\subsection{DistilBERT results}

\subsubsection{Model Configuration.}
We train DistilBERT (\texttt{distilbert-base-cased}) for NER token classification.
DistilBERT is a lightweight distilled variant of BERT with 6 layers, 768 hidden dimensions, and 66M parameters,
making it computationally efficient while retaining strong performance. The tokenizer is configured without
prefix spacing (unlike RoBERTa).

\subsubsection{Baseline: CE Loss.}

\begin{table}[H]
\centering
\caption{DistilBERT with Base Cross-Entropy Loss (5 epochs).}
\begin{tabular}{lcccc}
\hline
\textbf{Epoch} & \textbf{Train Loss} & \textbf{Val Loss} & \textbf{F1} & \textbf{Precision} / \textbf{Recall} / \textbf{Acc.} \\
\hline
1 & 0.2308 & 0.0940 & 0.8929 & 0.8809 / 0.9053 / 0.9697 \\
2 & 0.0763 & 0.0844 & 0.9027 & 0.9004 / 0.9050 / 0.9722 \\
3 & 0.0574 & 0.0817 & 0.9069 & 0.9021 / 0.9117 / 0.9737 \\
4 & 0.0480 & 0.0840 & 0.9104 & 0.9049 / 0.9160 / 0.9741 \\
5 & 0.0377 & 0.0859 & 0.9095 & 0.9046 / 0.9145 / 0.9744 \\
\hline
\end{tabular}
\label{tab:distilbert_base_ce}
\end{table}

The baseline cross-entropy approach achieves a final F1-score of \textbf{0.9095}, establishing a strong reference point for subsequent variants.
Training loss decreases smoothly from 0.23 to 0.04 over five epochs, indicating excellent convergence without instability.
Validation loss remains remarkably stable (0.084--0.094), suggesting minimal overfitting and robust generalization to unseen data.
Precision and recall are well-balanced (P $\approx$ 0.905, R $\approx$ 0.914), demonstrating that the model neither biases toward false positives nor false negatives.
With 97.44\% token-level accuracy, this baseline provides a reliable foundation against which to measure the contribution of more sophisticated loss functions.

---

\subsubsection{Focal Loss.}

\begin{table}[H]
\centering
\caption{DistilBERT with Focal Loss ($\gamma = 2.0$, weighted by class frequency).}
\begin{tabular}{lcccc}
\hline
\textbf{Epoch} & \textbf{Train Loss} & \textbf{Val Loss} & \textbf{F1} & \textbf{Precision} / \textbf{Recall} / \textbf{Acc.} \\
\hline
1 & 0.1784 & 0.0526 & 0.7528 & 0.6821 / 0.8399 / 0.9194 \\
2 & 0.0388 & 0.0435 & 0.8012 & 0.7504 / 0.8594 / 0.9440 \\
3 & 0.0263 & 0.0456 & 0.8265 & 0.7827 / 0.8755 / 0.9494 \\
4 & 0.0194 & 0.0438 & 0.8389 & 0.7956 / 0.8871 / 0.9546 \\
5 & 0.0141 & 0.0462 & 0.8447 & 0.8063 / 0.8869 / 0.9572 \\
\hline
\end{tabular}
\label{tab:distilbert_focal}
\end{table}

Focal Loss with class weighting yields a final F1 of \textbf{0.8447}, which is \textbf{0.0648 points lower} than the baseline CE loss.
This outcome is counterintuitive: despite being designed to handle class imbalance by downweighting easy examples and emphasizing hard negatives, Focal Loss underperforms significantly.
The model achieves lower precision (0.8063 vs. 0.9046 in CE) and comparable recall (0.8869 vs. 0.9145 in CE), indicating the loss function may be over-regularizing the model or creating conflicting gradient signals with the class weights.
Training loss decreases rapidly and steadily, but validation loss remains lowest among all variants (0.0462 at epoch 5), suggesting the model may be underfitting—focusing too heavily on hard examples while potentially losing general discriminative power.
The degraded performance suggests that class weighting alone is insufficient or even detrimental on this dataset, possibly because the tag distribution is already well-balanced or the Focal Loss penalty is too aggressive with $\gamma = 2.0$.

---

\subsubsection{Dice Loss.}

\begin{table}[H]
\centering
\caption{DistilBERT with Dice Loss ($\epsilon = 10^{-6}$).}
\begin{tabular}{lcccc}
\hline
\textbf{Epoch} & \textbf{Train Loss} & \textbf{Val Loss} & \textbf{F1} & \textbf{Precision} / \textbf{Recall} / \textbf{Acc.} \\
\hline
1 & 0.2118 & 0.1172 & 0.8599 & 0.8489 / 0.8712 / 0.9574 \\
2 & 0.1062 & 0.1028 & 0.8800 & 0.8839 / 0.8761 / 0.9654 \\
3 & 0.0918 & 0.0987 & 0.8883 & 0.8936 / 0.8831 / 0.9665 \\
4 & 0.0858 & 0.0964 & 0.8882 & 0.8887 / 0.8877 / 0.9679 \\
5 & 0.0822 & 0.0949 & 0.8911 & 0.8937 / 0.8884 / 0.9685 \\
\hline
\end{tabular}
\label{tab:distilbert_dice}
\end{table}

Dice Loss achieves a competitive F1 of \textbf{0.8911}, only \textbf{0.0184 points below} the CE baseline, while offering distinct advantages in metric optimization.
By directly optimizing an F1-like metric during training, Dice Loss produces excellent precision-recall balance (P = 0.8937, R = 0.8884 at epoch 5)—arguably better aligned with the F1 metric than CE loss.
The model reaches strong performance as early as epoch 1 (F1 = 0.8599) and plateaus by epoch 3--4, indicating rapid stabilization and efficient use of training epochs.
Validation loss increases slightly from epochs 2--5 (0.1028 $\to$ 0.0949), which is within acceptable bounds and does not indicate severe overfitting.
Token-level accuracy of 96.85\% is the highest among all single-loss variants tested, demonstrating Dice Loss's ability to maintain discriminative power across all tokens.
While Dice Loss underperforms CE by 1.84\% F1, its superior balance and early convergence make it valuable for applications where precision-recall trade-offs matter.

---

\subsubsection{CE + Dice Loss.}

\begin{table}[H]
\centering
\caption{DistilBERT with Combined CE + Dice Loss.}
\begin{tabular}{lcccc}
\hline
\textbf{Epoch} & \textbf{Train Loss} & \textbf{Val Loss} & \textbf{F1} & \textbf{Precision} / \textbf{Recall} / \textbf{Acc.} \\
\hline
1 & 0.4448 & 0.2078 & 0.8880 & 0.8720 / 0.9046 / 0.9689 \\
2 & 0.1748 & 0.1844 & 0.9050 & 0.9023 / 0.9078 / 0.9724 \\
3 & 0.1411 & 0.1805 & 0.9069 & 0.9039 / 0.9100 / 0.9737 \\
4 & 0.1207 & 0.1798 & 0.9119 & 0.9076 / 0.9163 / 0.9744 \\
5 & 0.1051 & 0.1805 & 0.9109 & 0.9066 / 0.9152 / 0.9746 \\
\hline
\end{tabular}
\label{tab:distilbert_ce_dice}
\end{table}

The combined CE + Dice Loss approach achieves the \textbf{highest F1-score of 0.9109} among all DistilBERT variants, surpassing the CE baseline by \textbf{0.0014 points} and Dice alone by \textbf{0.0198 points}.
This variant demonstrates the complementary benefits of both losses: cross-entropy provides stable, well-calibrated class probabilities, while Dice directly optimizes the F1 metric and ensures balanced precision-recall.
The model shows rapid convergence, jumping from F1 = 0.8880 (epoch 1) to 0.9050 (epoch 2), indicating synergistic learning signals from the combined objective.
Training loss is higher initially (0.4448 at epoch 1) due to summing two losses, but this is expected and poses no optimization challenge.
Validation loss stabilizes smoothly around 0.18 after epoch 2, indicating controlled learning without overfitting.
The precision-recall balance is excellent (P = 0.9066, R = 0.9152), with recall slightly higher than precision, contributing to the strong F1.
Token accuracy reaches 97.46\%, tied with the best-performing variant.
Despite the marginal improvement over CE alone (0.14\% F1), the CE + Dice combination represents the most robust and reliable architecture for this dataset, offering both high absolute performance and excellent stability.

---

\subsubsection{CRF Layer.}

\begin{table}[H]
\centering
\caption{DistilBERT + CRF (no focal loss).}
\begin{tabular}{lcccc}
\hline
\textbf{Epoch} & \textbf{Train Loss} & \textbf{Val Loss} & \textbf{F1} & \textbf{Precision} / \textbf{Recall} / \textbf{Acc.} \\
\hline
1 & 6.2858 & 2.1382 & 0.8846 & 0.8713 / 0.8985 / 0.9677 \\
2 & 1.8468 & 1.9222 & 0.8971 & 0.8915 / 0.9027 / 0.9713 \\
3 & 1.3623 & 1.8682 & 0.8990 & 0.8898 / 0.9084 / 0.9717 \\
4 & 1.1910 & 1.8535 & 0.9046 & 0.8957 / 0.9136 / 0.9735 \\
5 & 0.9343 & 1.8865 & 0.9064 & 0.9003 / 0.9126 / 0.9738 \\
\hline
\end{tabular}
\label{tab:distilbert_crf}
\end{table}

The CRF variant produces an F1 of \textbf{0.9064}, which is \textbf{0.0031 points below} the CE + Dice best and \textbf{0.0031 points below} the CE baseline.
The CRF architecture introduces a sequence-level loss function that explicitly models transition probabilities between adjacent tags, hence the markedly different training loss values (6.29 at epoch 1, compared to ~0.2 for token-level losses).
This higher absolute loss is not problematic and reflects the different optimization landscape of sequence prediction.
Training loss decreases smoothly from 6.29 to 0.93 over five epochs, and validation loss stabilizes around 1.89, indicating convergence without overfitting despite the different scale.
CRF achieves excellent precision and recall balance (P = 0.9003, R = 0.9126 at epoch 5), demonstrating that sequence-level constraints naturally enforce balanced token-level decisions.
Token accuracy of 97.38\% is slightly lower than CE + Dice but still competitive.
Surprisingly, enforcing label sequence validity through CRF does not yield measurable F1 improvements, suggesting that the baseline softmax predictions already contain mostly valid tag sequences on this well-behaved NER dataset.
The CRF model remains valuable for applications requiring explicit control over impossible label transitions, even if the empirical F1 gain is negligible.

---

\subsubsection{CRF + Focal Loss.}

\begin{table}[H]
\centering
\caption{DistilBERT + CRF with Focal Loss ($\gamma = 2.0$).}
\begin{tabular}{lcccc}
\hline
\textbf{Epoch} & \textbf{Train Loss} & \textbf{Val Loss} & \textbf{F1} & \textbf{Precision} / \textbf{Recall} / \textbf{Acc.} \\
\hline
1 & 6.0633 & 1.8933 & 0.8858 & 0.8724 / 0.8996 / 0.9679 \\
2 & 1.6268 & 1.6997 & 0.8959 & 0.8898 / 0.9021 / 0.9707 \\
3 & 1.1657 & 1.6224 & 0.8979 & 0.8896 / 0.9063 / 0.9717 \\
4 & 1.0049 & 1.5945 & 0.9030 & 0.8940 / 0.9122 / 0.9733 \\
5 & 0.7580 & 1.6152 & 0.9049 & 0.8982 / 0.9117 / 0.9736 \\
\hline
\end{tabular}
\label{tab:distilbert_crf_focal}
\end{table}

CRF + Focal Loss achieves an F1 of \textbf{0.9049}, placing it \textbf{0.0060 points below} the CE + Dice best variant and \textbf{0.0046 points below} pure CRF.
The combination of CRF's sequence-level structure and Focal Loss's hard example emphasis does not yield synergistic benefits and may introduce conflicting optimization pressures.
Training loss decreases smoothly from 6.06 to 0.76 (the lowest among CRF variants), but this does not translate to superior validation performance, suggesting the model may be overfitting to hard examples identified by Focal Loss.
Validation loss decreases from 1.89 to 1.62 (epochs 1--3) then stabilizes around 1.61--1.62, indicating controlled learning.
Precision and recall remain balanced (P = 0.8982, R = 0.9117 at epoch 5), though slightly lower than pure CRF.
Token accuracy of 97.36\% is marginally lower than pure CRF (97.38\%).
The empirical results suggest that Focal Loss is counterproductive when combined with CRF. This may be because CRF already captures sequence structure and hard decisions at the sequence level, whereas Focal Loss adds redundant token-level hard example weighting. The two objectives appear to work at cross-purposes: Focal Loss amplifies gradients for difficult tokens, while CRF finds globally optimal sequences, potentially leading to suboptimal joint learning.
\textbf{Conclusion:} CRF + Focal Loss should not be recommended for this NER task.

\subsubsection{Summary: All DistilBERT Variants.}

\begin{table}[htbp]
\centering
\caption{DistilBERT: Final F1-scores Across All Loss Functions (epoch 5).}
\begin{tabular}{lcccc}
\hline
\textbf{Architecture} & \textbf{F1-score} & \textbf{Precision} & \textbf{Recall} & \textbf{Accuracy} \\
\hline
CE + Dice Loss & \textbf{0.9109} & \textbf{0.9066} & \textbf{0.9152} & \textbf{0.9746} \\
Base CE & 0.9095 & 0.9046 & 0.9145 & 0.9744 \\
CRF & 0.9064 & 0.9003 & 0.9126 & 0.9738 \\
CRF + Focal Loss & 0.9049 & 0.8982 & 0.9117 & 0.9736 \\
Dice Loss & 0.8911 & 0.8937 & 0.8884 & 0.9685 \\
Focal Loss & 0.8447 & 0.8063 & 0.8869 & 0.9572 \\
\hline
\end{tabular}
\label{tab:distilbert_summary}
\end{table}

\paragraph{Overall Performance Ranking.}
The six DistilBERT variants exhibit a clear hierarchy of performance.
\textbf{CE + Dice Loss emerges as the top performer} with $F1 = 0.9109$, establishing itself as the optimal choice for this NER task.
The performance gap between the best and worst variant is substantial: $0.9109 - 0.8447 = 0.0662$ (6.62 percentage points), highlighting the significant impact of loss function design on final performance.

The top three variants (CE + Dice, Base CE, CRF) cluster tightly within 0.0045 F1 points, with all achieving $F1 \geq 0.9064$.
This suggests that for DistilBERT on this dataset, sophisticated sequence modeling (CRF) and F1-aware losses (Dice) provide only marginal improvements over the simple baseline—a testament to the strong baseline and well-structured nature of the NER task.

In contrast, Focal Loss ($F1 = 0.8447$) and Dice Loss alone ($F1 = 0.8911$) show significantly degraded performance.
Focal Loss's poor performance ($-6.48\%$ relative to CE + Dice) is particularly noteworthy, as it suggests that class weighting and hard example emphasis are either unnecessary or harmful on a balanced dataset.
Dice Loss performs moderately ($-1.98\%$ relative to CE + Dice), indicating that while F1-aware optimization is beneficial in isolation, it achieves superior results when combined with cross-entropy rather than used alone.

\paragraph{Precision-Recall Trade-offs.}
Examining the precision-recall curves reveals important trade-offs:

\begin{itemize}
\item \textbf{CE + Dice Loss} achieves the best balance ($P = 0.9066$, $R = 0.9152$), with recall slightly exceeding precision, resulting in the highest $F1$.
\item \textbf{Base CE} and \textbf{CRF} maintain comparable balances ($P \approx 0.9025$, $R \approx 0.9136$), differing only marginally from CE + Dice.
\item \textbf{Dice Loss alone} exhibits reversed bias ($P = 0.8937$, $R = 0.8884$), where precision exceeds recall, suggesting the Dice objective favors entity detection over boundary accuracy.
\item \textbf{Focal Loss} severely degrades precision ($P = 0.8063$), indicating it over-emphasizes recall ($R = 0.8869$) at the expense of precision, likely due to aggressive down-weighting of easy examples.
\item \textbf{CRF + Focal Loss} falls between CRF and Focal Loss in precision ($P = 0.8982$), inheriting some precision loss from Focal Loss despite CRF's structural constraints.
\end{itemize}

\paragraph{Convergence and Stability.}
All variants exhibit smooth, monotonic improvement in $F1$ across epochs, with convergence achieved by epoch 3--4.
\textbf{CE + Dice Loss} demonstrates the most dramatic jump in $F1$ from epoch 1 ($0.8880$) to epoch 2 ($0.9050$), indicating efficient learning from the combined loss signals.
\textbf{Focal Loss} and \textbf{Dice Loss} show slower convergence and lower epoch-wise $F1$ values throughout training, suggesting these objectives require more careful tuning or are fundamentally misaligned with the task structure.

Validation loss patterns confirm stability: CE + Dice, Base CE, CRF, and CRF + Focal Loss all stabilize after epoch 2 with minimal fluctuation, whereas Focal Loss maintains the lowest absolute validation loss but yields the worst $F1$, indicating a disconnect between loss minimization and metric maximization.

\paragraph{Conclusion.}
DistilBERT with CE + Dice Loss represents the optimal configuration among the tested variants, achieving state-of-the-art performance on this NER task with $F1 = 0.9109$, precision $= 0.9066$, recall $= 0.9152$, and token accuracy $= 97.46\%$.
The narrow performance gap between the top variants (CE + Dice, Base CE, CRF) underscores that the baseline is already highly effective, leaving limited room for improvement via advanced techniques.
More sophisticated losses and decoding strategies contribute only marginal gains, and in the case of Focal Loss, degrade performance.
These findings suggest that the NER task at hand exhibits characteristics favorable to standard approaches: relatively balanced class distribution, clean label sequences, and sufficient model capacity in DistilBERT to capture linguistic patterns without special handling of imbalance or sequence constraints.

\subsection{Model Comparison and Discussion}